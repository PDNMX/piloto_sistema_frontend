import { Dictionary, JsonPath } from '@stoplight/types';
import { RunRuleCollection } from '../../types';
import { RulesetExceptionCollection } from '../../types/ruleset';
export interface IExceptionLocation {
    source: string;
    path: JsonPath;
}
export declare const pivotExceptions: (exceptions: RulesetExceptionCollection, runRules: RunRuleCollection) => Dictionary<IExceptionLocation[], string>;
